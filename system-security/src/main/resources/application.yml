server:
  port: 8082

spring:
  application:
    name: system-security
  datasource:
    username: 'admin'
    url: jdbc:postgresql://localhost:5432/system-security
    password: 'password'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
    database-platform: org.hibernate.dialect.PostgreSQLDialect